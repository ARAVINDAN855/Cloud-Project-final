# name: Build and Push to ACR

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Log in to Azure Container Registry
#         uses: azure/docker-login@v1
#         with:
#           login-server: translatorregistry2026.azurecr.io
#           username: ${{ secrets.ACR_USERNAME }}
#           password: ${{ secrets.ACR_PASSWORD }}

#       - name: Build Docker Image
#         run: |
#           docker build -t translatorregistry2026.azurecr.io/translator-frontend:v1 .
          
#       - name: Push to ACR
#         run: |
#           docker push translatorregistry2026.azurecr.io/translator-frontend:v1
          

name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          ###### App changes ######
          app_location: "/" # Your source code location
          api_location: "" # Api source code location - optional
          output_location: "dist" # IMPORTANT: Vite uses 'dist', not 'build'
          ###### End of app changes ######